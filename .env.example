# === VERITABANI ===
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=auth_service

# === JWT SECRETS ===
ACCESS_TOKEN_SECRET=your_super_secret_access_token_key
REFRESH_TOKEN_SECRET=your_super_secret_refresh_token_key

# === SERVER ===
PORT=3000
NODE_ENV=development

# === CORS ===
ALLOWED_DOMAINS=localhost:3000,localhost:3001,sales.local,support.local

# === COOKIE DOMAIN (Shared!) ===
COOKIE_DOMAIN=localhost              # Local development
# COOKIE_DOMAIN=.example.com         # Production (note: dot at start!)